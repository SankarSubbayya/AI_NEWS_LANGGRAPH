# Requirements for New Features
# Cancer-specific news sources, enhanced prompts, and alternative APIs

# RSS feed parsing for medical journals
feedparser>=6.0.10

# HTML parsing for web scraping
beautifulsoup4>=4.12.0

# HTTP requests for APIs
requests>=2.31.0

# YAML configuration parsing
PyYAML>=6.0.1

# Existing dependencies (should already be installed)
langchain>=0.1.0
langchain-openai>=0.0.5
langgraph>=0.0.20
pydantic>=2.0.0
streamlit>=1.29.0

# Optional: Additional news APIs
# tavily-python>=0.3.0  # AI-optimized search (fallback)
# newsapi-python>=0.2.7  # NewsAPI.org integration
